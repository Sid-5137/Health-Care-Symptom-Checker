# Backend config
HF_TOKEN=
MODEL_NAME=meta-llama/Llama-3.1-70B-Instruct
# Point backend to the shared Django SQLite DB (default). Override if needed.
# DATABASE_URL=sqlite:///C:/path/to/Healthcare_Symptom_Checker/frontend/db.sqlite3

# Frontend config (optional)
# DEBUG=True
